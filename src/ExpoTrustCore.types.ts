/**
 * Type definitions for Expo Trust Core Module
 * Re-export from types/index.ts for compatibility
 */

export type {
  Wallet,
  Address,
  SigningInput,
  SigningOutput,
  ChainInfo,
  HDWalletOptions,
} from './types';
